services:
  postgres:
    image: postgres:18-alpine
    env_file:
      - .env
    ports:
      - 5555:5432
    volumes:
      - db:/var/lib/postgresql
    command: postgres -c listen-addresses='*'

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

volumes:
  db: